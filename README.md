# 02-react-cafe

Ready-to-run Vite + React + TypeScript project for the "React Cafe" feedback widget.

How to run:
1. npm install
2. npm run dev
3. Open http://localhost:5173

All components are in `src/components`. Types are in `src/types/votes.ts`.
Project UI is in English as requested.
